import { ColorCode } from './color-code';

describe('ColorCode', () => {
  it('should create an instance', () => {
    const directive = new ColorCode();
    expect(directive).toBeTruthy();
  });
});
